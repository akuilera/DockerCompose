PATH_TO_CONTAINERS=/path/to/containers
COLLABORA_PASSWORD=collabora_admin_password
# Allowlist of Nextcloud hosts that may reach Collabora (WOPI). Regex-escaped,
# separated by `|`. Ej: nextcloud\.example\.com|nextcloud\.local
COLLABORA_DOMAIN=nextcloud_domain_allowlist
