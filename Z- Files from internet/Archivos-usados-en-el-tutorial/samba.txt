version: '3.8'

services:
  samba:
    image: dperson/samba
    restart: unless-stopped
    container_name: samba
    ports:
      - "137:137/udp"
      - "138:138/udp"
      - "139:139/tcp"
      - "445:445/tcp"
    environment:
      - USERID=0 # UID del usuario
      - GROUPID=0 # GID del grupo
      - TZ=Etc/UTC   # Zona horaria
    volumes:
      - /mnt/usb/emm:/mnt/usb/emm
    command: '-u "emm;zxc" -s "emm;/mnt/usb/emm;yes;no;no;emm"'
   # command: >
   #   bash -c "
   #   chown -R 1000:1000 /mnt/usb/emm &&
   #   samba.sh -u 'emm;zxc' -s 'shared;/mnt/usb/emm;yes;no;no;emm'"
