# Copy as .env and fill with real values. DO NOT upload .env to the repo.
# Resolved from global.env at the repo root; see global.env.example.
PATH_TO_CONTAINERS=/path/to/your/containers/folder
PATH_TO_SECRETS=/path/to/your/secrets/folder
