services:
  jackett:
    image: lscr.io/linuxserver/jackett:latest
    container_name: jackett
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - AUTO_UPDATE=true #optional
      - RUN_OPTS= #optional
    volumes:
      - ${PATH_TO_CONTAINERS}/Jackett/data:/config
      - ${PATH_TO_DOWNLOADS}:/downloads
    ports:
      - 9117:9117
    restart: unless-stopped
