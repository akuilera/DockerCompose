Heimdall is going to be used as Dashboard.

You have to copy or rename the file `panel.txt` as `docker-compose.yml`

It is going to be using the folder `Heimdall` inside the specified path in the `.env` file. You have to specify it by copying or changing the name of the `env.example` file to `.env` and modify it.
