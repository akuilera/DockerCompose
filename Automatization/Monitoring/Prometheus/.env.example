PATH_TO_CONTAINERS=/path/to/containers/data
PATH_TO_SECRETS=/path/to/secrets

# Monitored hosts for the node job (file_sd), comma-separated host:port.
# Add one address per device; the server's own node-exporter is the default.
NODE_TARGETS=node-exporter:9100,node-client-1:9100
